# This file is no longer used for language enumeration.
# The pipeline automatically trains every language that has a .traineddata file in tessdata/.
#
# This file is kept only as documentation of script-to-language mappings.
# The actual mapping lives in LANG_GROUPS in training/scripts/generate_training_data.py.
#
# To add a new language: drop its .traineddata in tessdata/ and add its
# language code to LANG_GROUPS with the correct character group(s).
# Languages not in LANG_GROUPS still get universal characters (music notes, punctuation).
